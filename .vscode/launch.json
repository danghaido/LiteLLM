{
    "version": "0.2.0",
    "configurations": [
        
        {
            "name": "Run query (module)",
            "type": "debugpy",
            "request": "launch",
            "module": "LiteLLM.scripts.auto_query",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal",
            "justMyCode": true,
            "env": { "PYTHONPATH": "${workspaceFolder}" }
        }
    ]
}